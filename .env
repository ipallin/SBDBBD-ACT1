# Credenciales del administrador de Mongo (solo para desarrollo/lab). Usa valores fuertes y únicos.
MONGO_ROOT_USER=admin
MONGO_ROOT_PASS=ChangeThisAdminPass!2025

# Credenciales del usuario de aplicación con permisos limitados sobre la BD 'tienda'.
MONGO_APP_USER=api_user
MONGO_APP_PASS=ChangeThisApiPass!2025

# Contraseña de arranque para el usuario incorporado `elastic`.
ELASTIC_PASSWORD=ReplaceWithStr0ngBootPass!

# Usuario y contraseña que deberá usar la aplicación 
ELASTIC_APP_USER=app_user
ELASTIC_APP_PASS=ReplaceWithAppUs3rPass!

# Credenciales que la API espera para conectarse a Mongo (deben coincidir
# con el usuario de aplicación creado en la inicialización de Mongo).
# Se requieren por `fastapi-app/main.py` en tiempo de arranque.
MONGO_USER=api_user
MONGO_PASS=ChangeThisApiPass!2025

# Base de datos que se inicializa y se usa por defecto en los init scripts.
MONGO_INITDB_DATABASE=tienda

# Usuario adicional opcional con readWrite (p.ej., otra integración/servicio)
#MONGO_RW_USER=rw_user
#MONGO_RW_PASS=ChangeThisRwPass!2025